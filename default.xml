<?xml version="1.0" encoding="UTF-8"?>

<manifest>

  <!-- remote servers to query -->
  <remote name="openembedded"    alias="origin"  fetch="https://git.openembedded.org/" />
  <remote name="yocto"           alias="origin"  fetch="https://git.yoctoproject.org/" />
  <remote name="toolchain"       alias="origin"  fetch="https://github.com/kraj" />

  <!-- YOCTO & OE -->

  <!-- OpenEmbedded core layer and feature supportive layer -->
  <project name="openembedded-core"    path="openembedded-core"    remote="openembedded"  revision="wrynose"  upstream="wrynose" />
  <project name="bitbake"              path="bitbake"              remote="openembedded"  revision="2.18"  upstream="2.18" />
  <project name="meta-openembedded"    path="meta-openembedded"    remote="openembedded"  revision="wrynose"  upstream="wrynose" />

  <!-- meta-virtualization -->
  <project name="meta-virtualization"  path="meta-virtualization"  remote="yocto"  revision="wrynose"  upstream="wrynose"/>

  <!-- SELinux layer -->
  <project name="meta-selinux"         path="meta-selinux"         remote="yocto"  revision="wrynose"  upstream="wrynose"/>

<!-- meta-clang layer -->
  <project name="meta-clang"         path="meta-clang"         remote="toolchain"  revision="wrynose"  upstream="wrynose"/>
</manifest>
